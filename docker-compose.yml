services:
  backend:
    build: ./backend
    container_name: ho_backend
    ports:
      - "8000:8000"

  frontend:
    build: ./frontend
    container_name: ho_frontend
    ports:
      - "8501:8501"
    environment:
      - API_URL=http://backend:8000/recommend
    depends_on:
      - backend